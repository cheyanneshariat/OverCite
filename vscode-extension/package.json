{
  "name": "overcite-vscode",
  "displayName": "OverCite",
  "description": "Search ADS/SciX from LaTeX citation commands and insert BibTeX into local projects.",
  "version": "0.1.5",
  "publisher": "CheyanneShariat",
  "icon": "icon.png",
  "homepage": "https://github.com/cheyanneshariat/OverCite",
  "repository": {
    "type": "git",
    "url": "https://github.com/cheyanneshariat/OverCite.git"
  },
  "bugs": {
    "url": "https://github.com/cheyanneshariat/OverCite/issues"
  },
  "license": "MIT",
  "engines": {
    "vscode": "^1.90.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onCommand:overcite.resolveCitation",
    "onCommand:overcite.resolveCitationSimple",
    "onCommand:overcite.resolveCitationDirect",
    "onCommand:overcite.showDiagnostics"
  ],
  "main": "./src/extension.cjs",
  "type": "module",
  "contributes": {
    "commands": [
      {
        "command": "overcite.resolveCitation",
        "title": "OverCite: Resolve Citation"
      },
      {
        "command": "overcite.resolveCitationSimple",
        "title": "OverCite: Resolve Citation (Simple Search)"
      },
      {
        "command": "overcite.resolveCitationDirect",
        "title": "OverCite: Resolve Citation (ADS Query)"
      },
      {
        "command": "overcite.showDiagnostics",
        "title": "OverCite: Show Diagnostics"
      }
    ],
    "keybindings": [
      {
        "command": "overcite.resolveCitation",
        "key": "alt+shift+e",
        "when": "editorTextFocus"
      },
      {
        "command": "overcite.resolveCitationSimple",
        "key": "alt+shift+s",
        "when": "editorTextFocus"
      }
    ],
    "configuration": {
      "title": "OverCite",
      "properties": {
        "overcite.adsApiToken": {
          "type": "string",
          "default": "",
          "markdownDescription": "ADS/SciX API token used for search and BibTeX export."
        },
        "overcite.contextWindowChars": {
          "type": "number",
          "default": 500,
          "minimum": 200,
          "maximum": 1200,
          "markdownDescription": "Number of nearby characters to include as backup context around the current citation."
        },
        "overcite.citationKeyMode": {
          "type": "string",
          "default": "authoryear",
          "enum": [
            "authoryear",
            "informative",
            "bibcode",
            "typed"
          ],
          "markdownDescription": "Use a plain author-year key like `Shariat2025`, an informative key like `Shariat25_10k`, an ADS bibcode like `2024MNRAS.52711719Y`, or preserve the typed key when possible."
        },
        "overcite.bibliographyInsertMode": {
          "type": "string",
          "default": "append",
          "enum": [
            "append",
            "alphabetical"
          ],
          "markdownDescription": "Append new BibTeX entries at the end of the target `.bib` file or insert them alphabetically by final citation key."
        },
        "overcite.defaultSearchMode": {
          "type": "string",
          "default": "contextual",
          "enum": [
            "contextual",
            "simple",
            "direct"
          ],
          "markdownDescription": "Choose whether `OverCite: Resolve Citation` starts in contextual mode, simple search mode, or ADS query mode. The separate simple-search and ADS-query commands are still always available."
        },
        "overcite.projectBibFileOverrides": {
          "type": "object",
          "default": {},
          "markdownDescription": "Optional object keyed by workspace folder path. Values should be `.bib` filenames to force OverCite to use in that workspace."
        }
      }
    }
  },
  "scripts": {
    "test": "node --test",
    "check": "node --check src/extension.js && node --check src/service.js && node --check src/config.js && node --check src/extension.cjs && node --check tests/integration/run_integration.mjs && node --check tests/integration/suite.cjs",
    "test:integration": "node tests/integration/run_integration.mjs"
  },
  "devDependencies": {
    "@vscode/test-electron": "^2.5.2",
    "@vscode/vsce": "^3.7.1",
    "vscode": "^1.1.37"
  }
}
